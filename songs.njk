---
layout: default
permalink: "songs/"
title: Songs I Like
pageClass: songs
---

<h2 class="compilation-title">
  A List of
  <span class="compilation-count">{{ songs | length }}
    Songs</span>
  I Like</span>
</h2>

<article>
<ul>
  {% for song in songs %}
    <li>
      {% if song.link %}
        <a href="{{ song.link }}" title="Listen to {{ song.track | superSafeString }} by {{ song.artist | superSafeString }}">
      {% else %}
        <div>
      {% endif %}
          <span>"{{ song.track }}"</span>
          <span><span>by</span> {{ song.artist }}</span>
          <br/>
          <small><span>Notes:</span> {{ song.notes }}</small>
      {% if song.link %}
        </a>
      {% else %}
      </div>
      {% endif %}
  </li>
{% endfor %}
</ul>
</article>