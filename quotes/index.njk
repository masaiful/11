---
layout: default
pagination:
  data: collections.quotes
  size: 50
  alias: quotes
  reverse: true
permalink: "quotes/{% if pagination.pageNumber > 0 %}/{{ pagination.pageNumber + 1 }}/{% endif %}index.html"
title: Here & There
---

{% from "macros.njk" import singlePost %}

<div class="quotes-list">
  {% for thing in quotes %}
    {{ singlePost(thing) }}
  {% endfor %}
</div>